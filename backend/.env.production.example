# Production Environment Variables
# IMPORTANT: NEVER commit this file to git!

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=darkspace_app
DB_PASSWORD=CHANGE_THIS_TO_STRONG_PASSWORD
DB_NAME=darkspace_prod

# Server Configuration
PORT=3001
NODE_ENV=production

# Frontend URL (your domain)
CLIENT_URL=https://darkspacer.com

# Security Secrets (Generate new ones with: openssl rand -base64 64)
JWT_SECRET=CHANGE_THIS_TO_RANDOM_64_CHAR_STRING
SESSION_SECRET=CHANGE_THIS_TO_RANDOM_64_CHAR_STRING

# Optional: SSL/TLS for PostgreSQL
DB_SSL=true
